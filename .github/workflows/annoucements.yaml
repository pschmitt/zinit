name: 📣 Annoucements
on:
  discussion:
    types: [created, edited, pinned]


jobs:
  gitter-im-broadcast:
    runs-on: ubuntu-latest
    steps:
      - name: 📣 Broadcast announcement
        run: |
          echo 'New discussion!'
          echo "$GITHUB"
          echo "$GITHUB_EVENT"
          cat "$GITHUB_EVENT_PATH"
